# MIMII — Raspberry Pi 5 requirements (full edge stack)
# Install: pip install -r requirements_rpi.txt
# NOTE: Do NOT install PyTorch on Pi (use ONNX Runtime only)

-r requirements_edge.txt

# Optional HTTP API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.0.0

# Audio upload / resampling (inference_server /predict/audio)
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.11.0
