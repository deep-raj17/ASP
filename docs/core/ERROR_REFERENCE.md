# Workflow Error Reference

Stable errors include invalid definition/version/transition, duplicates, cycles,
unsatisfied prerequisites, missing evidence, missing approval, policy violation,
concurrency conflict, terminal workflow, idempotent no-change, state-store
failure, and audit-write failure. CLI mappings use nonzero documented codes.
