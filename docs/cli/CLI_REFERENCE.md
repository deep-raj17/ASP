# CLI Reference

The `ros` entry point provides init, project, status, verify, run, resume, gate,
workflow, evidence, registry, module, doctor, export, and archive. Use `--help`
and JSON format for automation.
