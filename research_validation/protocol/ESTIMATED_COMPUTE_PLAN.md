# Estimated Compute Plan

No runtime estimate is certified because hardware, dependency versions, and
approved training budget are not yet frozen. Before authorization, record the
device, CUDA/runtime versions, worker count, expected wall-clock range, storage
requirements, and abort criteria for each experiment family.
