# Acquisition Execution Authorization

**Status: AUTHORIZED / NOT STARTED**

Fresh acquisition may occur only under `C:\MIMII_VERIFIED_ACQUISITION`. The
historical `E:\MIMII` root must remain untouched. Each archive must be
downloaded to a temporary distinguishable path, verified by official MD5, and
only then extracted into the isolated `extracted` directory. Any mismatch,
missing file, storage shortfall, or extraction overwrite must stop the process.

PMPS-01 remains **BLOCKED — conflicting evidence** until all twelve archives,
fresh extraction, and file-level comparison are complete.
