# New Acquisition Root Certification

`C:\MIMII_VERIFIED_ACQUISITION` is a new isolated NTFS root with the required
`archives`, `extracted`, `checksums`, `comparison`, `logs`, `manifests`,
`evidence`, `temp`, and `downloads` directories. The write probe passed.

It is distinct from the protected historical root `E:\MIMII`. No download or
extraction has occurred.
