# CHAAD Contradiction Report

Preserved contradictions:

1. Historical PMPS `FAIL` versus ROS `BLOCKED`: the former used binary prompt
   semantics; the latter distinguishes missing evidence from proven failure.
2. An earlier manifest-sidecar mismatch statement versus currently matching
   bytes. The current match is verified; the correction origin is unknown.
3. Legacy near-perfect validation metrics versus corrected machine-independent
   validation ROC-AUC near 0.60. The legacy metric is superseded, not erased.
4. The original EXP-CHAAD-001 prediction export versus its corrected export.
   The original remains `INVALID`; the correction is provisional and
   validation-only.
5. Publication/camera-ready lifecycle language versus a conditional 57.1%
   publication audit and blocked PMPS-01. No readiness claim was imported.

No contradiction was resolved by deleting evidence or choosing a convenient
report.
